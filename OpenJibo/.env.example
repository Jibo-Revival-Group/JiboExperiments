# Copy to .env and set a strong password for the launch-rules admin UI.
OPENJIBO_LAUNCH_RULES_PASSWORD=change-me

# Optional: postgres password when using docker compose self-hosted profile
# OPENJIBO_POSTGRES_PASSWORD=change-me
